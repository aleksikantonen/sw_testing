[project]
name = "todo-list-app-api-tests"
version = "0.1.0"
description = "http api tests for the todo list app backend"
requires-python = ">=3.11"
dependencies = [
    "pytest==9.1.0",
    "httpx==0.28.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]